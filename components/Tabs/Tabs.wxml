<view class="tabs-title">
  <view class="tabs-item {{item.isActive?'active':''}}"
  bindtap='handleTap'
  data-index='{{index}}'
  wx:key="index"
  wx:for="{{tabs}}">
  {{item.title}}
  </view>
</view>
<view class="tabs-content">
  <slot></slot>
</view>
